version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    # Specify the directory where the build artifacts are saved
    baseDirectory: build
    files:
      - '**/*'
  # Add any other relevant configuration here, such as deploy settings
